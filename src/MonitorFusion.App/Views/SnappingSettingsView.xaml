<UserControl x:Class="MonitorFusion.App.Views.SnappingSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="0,0,10,0">
            <!-- Header -->
            <TextBlock Text="Window Snapping"
                       Foreground="{StaticResource TextBrush}"
                       FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <Border Background="{StaticResource SurfaceBrush}"
                    CornerRadius="8" Padding="16" Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="General Settings" Foreground="{StaticResource TextBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10"/>
                    
                    <CheckBox x:Name="EnabledCheck" Content="Enable Window Snapping" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                    
                    <StackPanel IsEnabled="{Binding ElementName=EnabledCheck, Path=IsChecked}">
                        <CheckBox x:Name="SnapToEdgesCheck" Content="Snap to Monitor Edges" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                        <CheckBox x:Name="SnapToWindowsCheck" Content="Snap to Other Windows" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                        <CheckBox x:Name="StickySnappingCheck" Content="Sticky Snapping (Immediate)" Foreground="{StaticResource TextBrush}" Margin="0,0,0,10" Checked="Setting_Changed" Unchecked="Setting_Changed"/>
                        <CheckBox x:Name="BypassShiftCheck" Content="Bypass Snapping when holding Shift" Foreground="{StaticResource TextBrush}" Margin="0,0,0,20" Checked="Setting_Changed" Unchecked="Setting_Changed"/>

                        <TextBlock Text="Snap Distance (pixels)" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,5"/>
                        <Grid Margin="0,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="40"/>
                            </Grid.ColumnDefinitions>
                            <Slider x:Name="DistanceSlider" Grid.Column="0" Minimum="5" Maximum="100" Value="20" TickFrequency="5" IsSnapToTickEnabled="True" VerticalAlignment="Center" ValueChanged="Setting_Changed"/>
                            <TextBlock Text="{Binding ElementName=DistanceSlider, Path=Value}" Foreground="{StaticResource TextBrush}" Grid.Column="1" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                        </Grid>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Ignored Processes -->
            <Border Background="{StaticResource SurfaceBrush}"
                    CornerRadius="8" Padding="16" Margin="0,0,0,20">
                <StackPanel IsEnabled="{Binding ElementName=EnabledCheck, Path=IsChecked}">
                    <TextBlock Text="Ignored Processes" Foreground="{StaticResource TextBrush}" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,5"/>
                    <TextBlock Text="These apps will not be affected by window snapping." FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,10"/>
                    
                    <ListBox x:Name="IgnoredList" Height="150" Margin="0,0,0,10" Background="#1A1A2E" Foreground="{StaticResource TextBrush}" BorderThickness="0"/>
                    
                    <WrapPanel>
                        <TextBox x:Name="ProcessInput" Width="200" Margin="0,0,10,0" Padding="4" VerticalContentAlignment="Center"/>
                        <Button Content="Add Process" Style="{StaticResource ModernButton}" Margin="0,0,10,0" Click="AddProcess_Click"/>
                        <Button Content="Remove Selected" Style="{StaticResource ModernButton}" Click="RemoveProcess_Click"/>
                    </WrapPanel>
                </StackPanel>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Save Options" Style="{StaticResource ModernButton}" Background="{StaticResource AccentBrush}" Click="Save_Click"/>
            </StackPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>
